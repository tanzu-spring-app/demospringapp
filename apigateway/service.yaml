apiVersion: v1
kind: Service
metadata:
  labels:
    app: demospringapp
    appGroup: MySpringApp
  name: apigateway-service
  namespace: default
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: demospringapp
  type: LoadBalancer