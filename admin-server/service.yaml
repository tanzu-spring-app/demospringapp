apiVersion: v1
kind: Service
metadata:
  labels:
    app: adminserver
    appGroup: MySpringApp
    purpose: precious
  name: admin-server-service
  namespace: voyager-ns
spec:
  ports:
  - port: 8089
    protocol: TCP
    targetPort: 8089
  selector:
    app: adminserver
  type: ClusterIP
status:
  loadBalancer: {}
